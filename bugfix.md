# Bugfix Notes

## 1. 记忆覆盖触发机制对偏好变更场景适配性差
实际观测场景
- 用户原话：“去年喜欢写 C++”
- 后续表述：“今年喜欢 Rust”
- 后生成的记忆无法覆盖前置旧记忆

当前问题成因
- 仅当新提取记忆先匹配到已有记忆候选条目时，才会触发覆盖逻辑。
- 现阶段候选匹配逻辑过度依赖向量相似度。
- 针对「随时间发生变化的偏好/立场/技术选择」类场景：两条记忆业务含义高度相关，但在向量嵌入空间相似度不足，无法进入冲突判定、覆盖替换流程。
- 记忆调和器目前仅校验匹配度最优的单条候选，不会围绕同一主题候选集、记忆演变脉络做综合推理。

问题影响
- 系统难以捕捉用户偏好的演变过程。
- 容易生成多条并行独立记忆，无法形成清晰的偏好变更脉络。
- 不利于后续对用户状态变迁、心态变化做数据分析。

可优化方向
1. 放宽阈值：同一主题下的候选记忆，即便向量相似度偏低，也可纳入冲突、覆盖校验流程。
2. 依托分类/标签/推理类型/时间特征，识别存在演变关联的记忆候选。
3. 将「仅取最优匹配」的逻辑改造为「感知记忆演变脉络的调和机制」。
4. 去重逻辑升级为「合并/更新同一条记忆演变脉络」，而非单纯删除重复数据。


## 2. 当前模型不支持「多个历史 memory 被一个新 memory 同时覆盖」

实际期望场景

- 用户去年喜欢 C++
- 用户去年喜欢 LangChain
- 用户今年明确说自己已经不喜欢这些东西了，或者表达了新的统一偏好
- 这时“今年这条新记忆”理论上应该能同时覆盖多条旧记忆

当前限制

- 现有 schema 是单链模型：
  - `supersedes: Option<Uuid>`
  - `superseded_by: Option<Uuid>`
- 这意味着一条新 memory 只能指向一个旧 memory。
- 当前版本链天然表达的是 `v1 -> v2 -> v3` 这种单链结构，而不是“多个旧节点汇聚到一个新节点”的扇入结构。

为什么这是问题

- 用户真实偏好变化往往不是一条旧记忆单独被替换，而是一组相关旧记忆被新的阶段性结论统一收束。
- 例如：
  - 过去喜欢某语言
  - 过去喜欢某框架
  - 现在整体转向另一种技术路线
- 如果不能表达“多旧合一新”，系统就只能：
  1. 只覆盖其中一条，留下其他历史偏好继续干扰检索
  2. 或者新增一条平行记忆，但无法真正完成链路归并

后续可优化方向

1. 把 supersede 从单链升级为关系模型，而不是单个 `supersedes/superseded_by` 指针。
2. 支持一个新 memory 对多个旧 memory 建立“覆盖/收束”关系。
3. 检索时以“当前有效结论”为主，同时保留被其覆盖的历史节点集合，供解释和用户变化分析使用。
4. 重新定义 lineage：
   - 不再只是版本链
   - 而是“记忆演变图”或“记忆收束链路”
