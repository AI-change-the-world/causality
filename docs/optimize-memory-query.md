1. 进行混合检索之后，通过特定的语法格式直接把检索结果返回用户
比如：
```markdown
record: 1
time：2025....
event: 用户点击了不喜欢的按钮
infer: memory中的结果

record: 2
...
```

2. 需要重新设计测试脚本，一个是多轮对话问答，但是多轮对话现在模型能力很强的时候基本上看不出能力，所以只能比较query命中以及消耗token，就东拉西扯做一堆互不相关的QA，然后接着某个话题继续问

3. scope也要抽离出来，每个scope需要有自己的描述，以及对应的scene
